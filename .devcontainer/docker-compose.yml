services:
  api:
    volumes:
      - .:/workspace:cached,z
    command: /bin/sh -c "while sleep 1000; do :; done"
    dns:
      - 8.8.8.8
      - 1.1.1.1
    healthcheck:
      test: ["CMD", "true"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 5s
